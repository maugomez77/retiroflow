# RetiroFlow Configuration
ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=postgresql+asyncpg://retiroflow:retiroflow@localhost:5432/retiroflow
RETIROFLOW_PORT=8000
RETIROFLOW_HOST=0.0.0.0
RETIROFLOW_ENV=development
# Optional: Open-Meteo (free, no key needed)
# Optional: DuckDuckGo (free, no key needed)
